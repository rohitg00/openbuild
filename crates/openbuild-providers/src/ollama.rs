// Ollama /api/chat adapter. M1 fills in.
// Note: Ollama also exposes OpenAI-compat at /v1/chat/completions — the OpenAi adapter
// works against it directly with empty api_key and base_url = "http://localhost:11434/v1".
